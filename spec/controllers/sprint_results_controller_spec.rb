require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe SprintResultsController do

  # This should return the minimal set of attributes required to create a valid
  # SprintResult. As you add validations to SprintResult, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    {}
  end

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # SprintResultsController. Be sure to keep this updated too.
  def valid_session
    {}
  end

  describe "GET index" do
    it "assigns all sprint_results as @sprint_results" do
      sprint_result = SprintResult.create! valid_attributes
      get :index, {}, valid_session
      assigns(:sprint_results).should eq([sprint_result])
    end
  end

  describe "GET show" do
    it "assigns the requested sprint_result as @sprint_result" do
      sprint_result = SprintResult.create! valid_attributes
      get :show, {:id => sprint_result.to_param}, valid_session
      assigns(:sprint_result).should eq(sprint_result)
    end
  end

  describe "GET new" do
    it "assigns a new sprint_result as @sprint_result" do
      get :new, {}, valid_session
      assigns(:sprint_result).should be_a_new(SprintResult)
    end
  end

  describe "GET edit" do
    it "assigns the requested sprint_result as @sprint_result" do
      sprint_result = SprintResult.create! valid_attributes
      get :edit, {:id => sprint_result.to_param}, valid_session
      assigns(:sprint_result).should eq(sprint_result)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new SprintResult" do
        expect {
          post :create, {:sprint_result => valid_attributes}, valid_session
        }.to change(SprintResult, :count).by(1)
      end

      it "assigns a newly created sprint_result as @sprint_result" do
        post :create, {:sprint_result => valid_attributes}, valid_session
        assigns(:sprint_result).should be_a(SprintResult)
        assigns(:sprint_result).should be_persisted
      end

      it "redirects to the created sprint_result" do
        post :create, {:sprint_result => valid_attributes}, valid_session
        response.should redirect_to(SprintResult.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved sprint_result as @sprint_result" do
        # Trigger the behavior that occurs when invalid params are submitted
        SprintResult.any_instance.stub(:save).and_return(false)
        post :create, {:sprint_result => {}}, valid_session
        assigns(:sprint_result).should be_a_new(SprintResult)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        SprintResult.any_instance.stub(:save).and_return(false)
        post :create, {:sprint_result => {}}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested sprint_result" do
        sprint_result = SprintResult.create! valid_attributes
        # Assuming there are no other sprint_results in the database, this
        # specifies that the SprintResult created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        SprintResult.any_instance.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, {:id => sprint_result.to_param, :sprint_result => {'these' => 'params'}}, valid_session
      end

      it "assigns the requested sprint_result as @sprint_result" do
        sprint_result = SprintResult.create! valid_attributes
        put :update, {:id => sprint_result.to_param, :sprint_result => valid_attributes}, valid_session
        assigns(:sprint_result).should eq(sprint_result)
      end

      it "redirects to the sprint_result" do
        sprint_result = SprintResult.create! valid_attributes
        put :update, {:id => sprint_result.to_param, :sprint_result => valid_attributes}, valid_session
        response.should redirect_to(sprint_result)
      end
    end

    describe "with invalid params" do
      it "assigns the sprint_result as @sprint_result" do
        sprint_result = SprintResult.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SprintResult.any_instance.stub(:save).and_return(false)
        put :update, {:id => sprint_result.to_param, :sprint_result => {}}, valid_session
        assigns(:sprint_result).should eq(sprint_result)
      end

      it "re-renders the 'edit' template" do
        sprint_result = SprintResult.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SprintResult.any_instance.stub(:save).and_return(false)
        put :update, {:id => sprint_result.to_param, :sprint_result => {}}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested sprint_result" do
      sprint_result = SprintResult.create! valid_attributes
      expect {
        delete :destroy, {:id => sprint_result.to_param}, valid_session
      }.to change(SprintResult, :count).by(-1)
    end

    it "redirects to the sprint_results list" do
      sprint_result = SprintResult.create! valid_attributes
      delete :destroy, {:id => sprint_result.to_param}, valid_session
      response.should redirect_to(sprint_results_url)
    end
  end

end
