
    today = Date.today
    # we want to show results of last 5 sprints
    @results = team
                .sprint_results
                .joins(:sprint)
                .where(["sprints.end < ? AND points_actual IS NOT ?", today, nil])
                .order("sprints.end DESC")i
                .limit(5)
    @actual_sprint_result = team
                            .sprint_results
                            .joins(:sprint)
                            .where["sprints.start <= ? AND sprints.end >= ?", today, today])
    @actual_sprint = Sprint.find(sprint_result_today.sprint_id)
